# Hacash Wallet v0.1.54

## Prices
- USD quotes come **only from CoinGecko** (`hacash` + `bitcoin`).
- Retries with backoff on HTTP 429 (free-tier rate limit).
- If CoinGecko is still unavailable, the home screen shows “USD quotes offline” (balances still work).
